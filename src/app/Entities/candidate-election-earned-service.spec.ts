import { CandidateElectionEarnedService } from './candidate-election-earned-service';

describe('CandidateElectionEarnedService', () => {
  it('should create an instance', () => {
    expect(new CandidateElectionEarnedService()).toBeTruthy();
  });
});
